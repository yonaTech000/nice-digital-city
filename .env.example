# Nice Digital City - Frontend Only Configuration
# This is a frontend-only application with localStorage persistence

# App Configuration
NEXT_PUBLIC_APP_NAME=Nice Digital City
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Note: No backend database required
# All data is mocked and stored in localStorage

# Future backend integration (when needed)
# DATABASE_URL="postgresql://user:password@localhost:5432/nice_city_db"
# NEXT_PUBLIC_API_URL="http://localhost:3000/api"
